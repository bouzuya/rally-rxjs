{
  "name": "rally-rxjs",
  "description": "rally-rxjs",
  "version": "0.3.0",
  "author": {
    "name": "bouzuya",
    "email": "m@bouzuya.net",
    "url": "http://bouzuya.net"
  },
  "bugs": {
    "url": "https://github.com/bouzuya/rally-rxjs/issues"
  },
  "dependencies": {
    "b-o-a": "0.2.0",
    "express": "^4.13.4",
    "htmlescape": "^1.1.0",
    "node-fetch": "^1.3.3",
    "path-to-regexp": "^1.2.1",
    "vdom-parser": "^1.2.1",
    "vdom-to-html": "^2.2.0",
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "intelli-espower-loader": "^1.0.1",
    "less": "^2.6.0",
    "mocha": "^2.3.4",
    "nodemon": "^1.9.1",
    "parallelshell": "^2.0.0",
    "power-assert": "^1.2.0",
    "typescript": "^1.7.5",
    "typings": "^0.7.7",
    "watch": "^0.17.1",
    "watchify": "^3.7.0"
  },
  "homepage": "https://github.com/bouzuya/rally-rxjs#readme",
  "keywords": [],
  "license": "MIT",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/bouzuya/rally-rxjs"
  },
  "scripts": {
    "build": "npm run build-ts && npm run cp && npm run build-browserify && npm run build-less",
    "build-browserify": "browserify -o dist/scripts/bundle.js client.js",
    "build-less": "lessc src/app/style/index.less dist/styles/index.css",
    "build-ts": "tsc",
    "clean": "rm -rf lib/ .tmp/ dist/ bundle.js",
    "cp": "cp -r .tmp/src/ lib/",
    "prebuild": "[ -d dist/scripts ] || mkdir -p dist/scripts",
    "prepublish": "typings install && npm run clean && npm run build",
    "start": "nodemon server.js",
    "test": "npm run build && mocha --recursive --require intelli-espower-loader .tmp/test/",
    "watch": "npm run build && parallelshell 'npm run watch-ts' 'npm run watch-cp' 'npm run watch-browserify' 'npm run watch-less'",
    "watch-browserify": "watchify -v -o dist/scripts/bundle.js client.js",
    "watch-cp": "watch --ignoreUnreadable --wait 2 'npm run cp' .tmp/src/",
    "watch-less": "watch --ignoreUnreadable --wait 2 'npm run build-less' src/app/style/",
    "watch-test": "mocha --watch --recursive --require intelli-espower-loader .tmp/test/",
    "watch-ts": "tsc --watch"
  }
}
